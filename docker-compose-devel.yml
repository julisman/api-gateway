api:
  image: secret06/nodestrongloop
  volumes:
    - .:/app
  working_dir: /app/auth-server
  ports:
      - "8701:8701"
      - "3000:3000"
  command: node server/server.js
  #command: slc start
 # command: slc pm
 # command: slc arc PORT=4000

 #  command: node server/server.js
